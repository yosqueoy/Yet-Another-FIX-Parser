export default class FixField {
    constructor (tag_name, tag, value, value_raw) {
        this.tag_name = tag_name;
        this.tag = tag;
        this.value = value;
        this.value_raw = value_raw;
    }
}
